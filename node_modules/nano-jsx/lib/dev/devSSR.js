"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
require("source-map-support/register");
const core_js_1 = require("../core.js");
const ssr_js_1 = require("../ssr.js");
const component_js_1 = require("../component.js");
const helmet_js_1 = require("../components/helmet.js");
// @ts-ignore
const fs_1 = __importDefault(require("fs"));
// @ts-ignore
const path_1 = require("path");
// @ts-ignore
const http_1 = __importDefault(require("http"));
class App extends component_js_1.Component {
    render() {
        return (0, core_js_1.h)("div", null, "Nano JSX App");
    }
}
const app = (0, ssr_js_1.renderSSR)((0, core_js_1.h)(App, null));
const { body, head, footer } = helmet_js_1.Helmet.SSR(app);
const html = `
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
      body { 
        font-family: BlinkMacSystemFont, -apple-system, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Fira Sans', 'Droid Sans', 'Helvetica Neue', Helvetica, Arial, sans-serif;
      }
     </style>
    ${head.join('\n')}
  </head>
  <body>
    <div id="root">
      ${body}
    </div>
  </body>
  ${footer.join('\n')}
</html>
`;
// minify
// html = html.replace(/[\s]+/gm, ' ')
http_1.default
    .createServer((req, res) => {
    const { url } = req;
    if (/\.html$/.test(url))
        return res.end(html);
    // @ts-ignore
    const path = (0, path_1.join)(__dirname, '../../', url);
    fs_1.default.readFile(path, (err, data) => {
        if (err) {
            res.writeHead(404);
            return res.end(data);
        }
        const type = /\.png$/.test(url) ? 'image/png' : 'image/svg+xml';
        res.setHeader('Content-Type', type);
        return res.end(data);
    });
})
    .listen(8080, () => console.log('open http://localhost:8080/index.html in your browser'));
//# sourceMappingURL=devSSR.js.map