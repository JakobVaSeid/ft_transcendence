import { _state } from '../state.js';
export const useState = (state, id) => {
    const s = {
        setState(state) {
            if (state !== null)
                _state.set(id, state);
        },
        get state() {
            return _state.get(id);
        }
    };
    if (!_state.has(id))
        _state.set(id, state);
    return [s.state, s.setState];
};
export const getState = (id) => {
    return _state.get(id);
};
export const setState = (id, state) => {
    return _state.set(id, state);
};
//# sourceMappingURL=useState.js.map