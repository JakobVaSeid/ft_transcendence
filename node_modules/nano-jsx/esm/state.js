/** Holds the state of the whole application. */
export const _state = new Map();
/** Clears the state of the whole application. */
export const _clearState = () => {
    _state.clear();
};
//# sourceMappingURL=state.js.map